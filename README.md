# CS2 Jailbreak Plugin

CounterStrikeSharp 기반 CS2 감옥서버 진행 보조 플러그인입니다.

현재 버전은 `0.2.23`입니다. 서버 안정성을 우선해서 LR, 1:1 감지, 비콘, 고문시간 기능은 제거했고, 간수 지시, 5초 카운트다운, 프리킬 시간, 자유시간, 반란자, 최근 사건, 간수 비율, 상호 동의 팀교환을 중심으로 정리합니다.

## 주요 기능

- 간수 지시 메뉴와 중앙 HUD
- 자유 입력 지시
- `$` 빠른 시간 지시
- `+` 추가명령
- `!5` 간수 카운트다운
- 간수 1명 남음 프리킬 시간 알림
- 전체/개인 자유시간
- 반란자 지정과 자동 반란 처리
- 최근 사건 기록
- 간수 비율 제한
- 상호 동의 팀교환
- 봇 참가자 판정 분리
- 관리자 상태 확인과 강제 초기화
- 미니맵 비활성화 시도

## 구현 현황

### 구현됨

| 분류 | 기능 | 상태 |
| --- | --- | --- |
| 간수 지시 | `!지시`, `/지시`, `css_jb order` 메뉴 | 구현 완료 |
| 간수 지시 | 중앙 HUD 상시 표시 | 구현 완료 |
| 간수 지시 | 자유 입력 지시 | 구현 완료 |
| 간수 지시 | `$6:30 중앙 앉아서` 형식 빠른 시간 지시 | 구현 완료 |
| 간수 지시 | `+점프 금지` 형식 추가명령 | 구현 완료 |
| 카운트다운 | `!5`, `/5`, `css_jb countdown` | 구현 완료 |
| 자유시간 | 라운드 시작 자동 3분 자유시간 | 구현 완료 |
| 자유시간 | 관리자 전체 자유시간 시작/종료 | 구현 완료 |
| 자유시간 | 개인 자유시간 지정/해제 | 구현 완료 |
| 프리킬 시간 | 라운드 시작 간수 3명 이상, 이후 간수 1명 남음 감지 | 구현 완료 |
| 프리킬 시간 | 프리킬 HUD 최우선 표시 | 구현 완료 |
| 반란자 | 죄수가 간수 공격 시 반란자 처리 | 구현 완료 |
| 반란자 | 반란자 빨간색 표시 및 중복 Render 재기록 최소화 | 구현 완료 |
| 최근 사건 | 카운트다운, 프리킬, 반란자 사건 기록 | 구현 완료 |
| 팀 관리 | 간수 비율 제한 | 구현 완료 |
| 팀 관리 | 상호 동의 팀교환 | 구현 완료 |
| 관리자 | `css_jb status`, `css_jb reset` | 구현 완료 |
| 기타 | 미니맵 비활성화 명령 실행 | 구현 완료 |
| 안정화 | 라운드 종료, 팀 변경, 퇴장, 맵 변경 시 상태 정리 | 구현 완료 |

### 테스트 필요

| 분류 | 확인할 내용 |
| --- | --- |
| 간수 지시 | 실제 서버에서 CT만 지시 가능하고 T는 차단되는지 |
| 간수 지시 | 중앙 HUD가 메뉴, 채팅, 사망 상태에서도 과하게 깜빡이지 않는지 |
| 자동 자유시간 | 라운드 시작 후 3분 자유시간이 시작/종료되는지 |
| 프리킬 시간 | 자동 자유시간 중 간수 1명 남음 상황에서 프리킬 HUD가 자유시간보다 우선하는지 |
| 프리킬 시간 | 프리킬 중 `!지시`, `+`, `!5`, 자유시간 시작이 HUD를 덮지 않는지 |
| 팀교환 | 요청, 수락, 거절, 거절 후 쿨다운이 정상 동작하는지 |
| 반란자 | 스킨 플러그인과 함께 쓸 때 빨간색 표시가 유지되는지 |
| 배포 | 서버 실행 중 DLL 교체 없이, 서버 종료 후 배포하면 상태 꼬임이 없는지 |

### 구현 예정

| 분류 | 내용 | 비고 |
| --- | --- | --- |
| 팀밸 | 라운드 시작/종료 기준 자동 팀 분리 | 룰 확정 후 구현 |
| 팀밸 자유시간 | 간수-죄수 인원 동일 시 자유시간 처리 | 죽은 간수 포함 여부 확인 필요 |
| 간수 역할 | 간수장, 쉴더 등 역할/능력 | 인원수 제한 설계 필요 |
| 데모 | 영상 녹화 안내 또는 명령 연동 | 서버 플러그인 구성 확인 필요 |
| 사운드 | 반란자, 프리킬 등 상황별 사운드 | soundevent 후보 확인 필요 |
| 메뉴 | 라디오 키 기반 간수 명령 메뉴 | CounterStrikeSharp/서버 메뉴 플러그인 제약 확인 필요 |
| Localization | 하드코딩 문구 정리 | 후순위 |

### 보류/제거됨

| 기능 | 상태 | 이유 |
| --- | --- | --- |
| LR | 제거 | 서버 안정성 우선, 반복 장비/데미지 훅 비용 문제 |
| 1:1 감지 | 제거 | 서버 안정성 우선 정리 |
| 비콘 | 제거 | CBeam 엔티티가 PVS 크래시 유발 |
| 고문시간 | 제거 | 정확한 서버 룰 미확정 |
| MOTD | 보류 | CS2 기본 MOTD 구현 제약 |
| 팀밸 자유시간 | 보류 | 발동 기준과 죽은 간수 포함 여부 미확정 |

## 운영 정책

### LR 제거

LR은 현재 빌드에서 제거했습니다.

CS2 서버 안정성을 우선하기 위해 LR 메뉴, LR 데미지 차단, LR 무기 지급, 줌 차단, 마지막 죄수 자동 감지는 등록하지 않습니다. `Features/LastRequest` 구현도 제거되어 빌드 대상에 포함되지 않습니다.

### 간수 지시

- `!지시`, `/지시`, `!order`, `/order`로 메뉴를 엽니다.
- 관리자 권한자가 아니면 CT 간수만 지시할 수 있습니다.
- T 상태에서는 지시할 수 없습니다.
- 살아있음 여부는 보지 않습니다. CS2에서 `PawnIsAlive` 판정이 순간적으로 흔들려 지시가 막히던 문제가 있었기 때문입니다.
- 새 지시가 나오면 전체 자유시간은 종료됩니다.
- 라운드 중에는 지시가 없어도 중앙 HUD에 `자유시간: 없음`, `추가명령: 없음`, `최근사건: 없음`을 표시합니다.

### `$` 빠른 시간 지시

일반 채팅에서 `$`로 시작하면 CSS식 빠른 시간 지시로 처리합니다.

```text
$6:30 중앙 앉아서
$6 중앙 앉아서
$90 중앙 앉아서
```

위 예시는 각각 `6:30까지 중앙 앉아서`, `6:00까지 중앙 앉아서`, `1:30까지 중앙 앉아서`로 표시됩니다.

시간은 현재 남은 라운드 시간보다 작아야 하며, 기존 간수 지시 쿨다운과 최소 시간 제한을 그대로 사용합니다.

자유 입력 메뉴에서 입력한 내용은 `$`로 시작하더라도 파싱하지 않고 그대로 간수 지시 HUD에 표시합니다.

### `+` 추가명령

일반 채팅에서 `+`로 시작하면 추가명령 줄을 갱신합니다.

```text
+점프 금지
+총 줍지 말기
+
```

빈 `+`는 추가명령을 `없음`으로 되돌립니다.

### `!5` 카운트다운

- `!5`, `/5`, `css_jb countdown`, `css_jb 5`로 5초 카운트다운을 시작합니다.
- 간수 또는 권한 있는 관리자만 사용할 수 있습니다.
- 기존 카운트다운이 있으면 새 카운트다운으로 교체됩니다.
- 모든 사람에게 중앙 메시지와 채팅 메시지를 보냅니다.
- 최근 사건에 카운트다운 시작 기록을 남깁니다.

### 프리킬 시간

- 라운드 시작 시 살아 있는 간수가 3명 이상이었고, 이후 살아 있는 간수가 1명으로 줄어드는 순간 자동으로 시작됩니다.
- 살아 있는 죄수가 1명 이상 있을 때만 시작합니다.
- 전체 자유시간 중에도 프리킬 조건이 충족되면 프리킬 시간이 우선합니다.
- 프리킬 시간이 시작되면 자동 자유시간 타이머와 자유시간 HUD를 종료합니다.
- 프리킬 시간 중에는 간수 지시, 추가명령, 최근사건 대신 중앙 HUD에 `프리킬 시간 입니다!`를 고정 표시합니다.
- 프리킬 시간 중에는 전체 자유시간과 카운트다운을 새로 시작할 수 없습니다.
- 매초 감시하지 않고 라운드 시작, 사망, 팀 변경 같은 이벤트 후에만 한 번 계산합니다.
- 짧은 시간에 사망/팀 변경/퇴장이 여러 번 발생해도 프리킬 평가는 한 번만 예약합니다.
- 시작 시 중앙 메시지, 채팅, 최근 사건에 기록합니다.

### 고문시간 제거

고문시간은 정확한 서버 룰을 더 확인하기 전까지 현재 빌드에서 제거했습니다.

`!고문`, `!고문끝`, `css_jb torture` 계열 명령은 등록하지 않습니다. 죄수 1명 남음 자동 감지도 하지 않습니다.

### 최근 사건

최근 사건은 라운드 중 운영자가 상황을 되짚어 볼 수 있게 짧게 저장합니다.

현재 기록되는 사건:

- 누가 `!5` 카운트다운을 시작했는지
- 프리킬 시간이 시작/종료됐는지
- 누가 자동 반란자가 되었는지
- 관리자가 누굴 반란자로 지정/해제했는지

`css_jb incidents`, `css_jb 사건`, `css_jb 최근사건`으로 확인합니다. 라운드 시작과 맵 시작, 강제 초기화 때 기록은 비웁니다.

### 자유시간

- 전체 자유시간은 관리자 명령으로 시작/종료합니다.
- 개인 자유시간은 살아 있는 죄수에게 지정할 수 있습니다.
- 반란자가 되면 개인 자유시간은 제거됩니다.

### 반란자

- 죄수가 간수를 공격하면 반란자로 표시됩니다.
- 반란자는 빨간색 렌더 컬러를 적용합니다.
- 이미 빨간색이면 매초 `Render` 재기록을 건너뛰어 불필요한 네트워크 상태 변경을 줄입니다.
- 라운드 종료, 팀 변경, 퇴장, 초기화 때 상태를 정리합니다.

### 팀교환

- 죄수와 간수 사이에서만 상호 동의 팀교환을 요청할 수 있습니다.
- 요청자는 `css_jb team swap <대상>` 또는 `!교환 <대상>`을 사용합니다.
- 대상자는 `css_jb team accept`, `!수락`으로 수락하거나 `css_jb team deny`, `!거절`로 거절합니다.
- 요청은 기본 20초 뒤 만료됩니다.
- 한 번 거절된 요청자-대상 조합은 기본 60초 동안 다시 요청할 수 없습니다.
- 라운드 시작, 라운드 종료, 맵 변경, 초기화, 퇴장 때 요청 상태를 정리합니다.
- 팀밸 자유시간은 죽은 간수 포함 여부 등 서버 룰이 확정되지 않아 아직 구현하지 않습니다.

## 명령어

플레이어 채팅:

```text
!지시
!5
!교환 <대상>
!수락
!거절
!도움말
```

관리 명령:

```text
css_jb help
css_jb version
css_jb status
css_jb reset

css_jb order
css_jb order cancel

css_jb countdown
css_jb incidents

css_jb team swap <대상>
css_jb team accept
css_jb team deny

css_jb freeday start
css_jb freeday end
css_jb freeday give <대상>
css_jb freeday remove <대상>
css_jb freeday list

css_jb rebel add <대상>
css_jb rebel remove <대상>
css_jb rebel list
```

한국어 하위 명령 일부도 지원합니다.

```text
css_jb 상태
css_jb 초기화
css_jb 지시 취소
css_jb 사건
css_jb 최근사건
css_jb 자유시간 시작
css_jb 반란 지정 <대상>
```

## 설정

예시는 `Jailbreak.example.json`을 참고합니다.

`PrisonersPerGuard`
: 인간 죄수 몇 명당 인간 간수 1명을 허용할지 정합니다. 봇은 비율 계산에서 제외합니다.

`AdminPermissions`
: 관리자 명령 허용 권한입니다.

`CustomOrderInputTimeoutSeconds`
: 자유 입력 지시 입력 대기 시간입니다.

`DisableRadar`
: 플러그인 로드/맵 시작 때 레이더 비활성화 명령을 실행할지 여부입니다.

`DisableRadarCommand`
: 레이더 비활성화에 사용할 서버 명령입니다.

`AutoFreedayEnabled`
: 라운드 시작 때 전체 자유시간을 자동으로 시작할지 정합니다.

`AutoFreedayDurationSeconds`
: 자동 자유시간 유지 시간입니다. 기본값은 180초입니다.

`TeamSwapRequestTimeoutSeconds`
: 팀교환 요청 만료 시간입니다. 기본값은 20초입니다.

`TeamSwapDeclineCooldownSeconds`
: 팀교환 요청을 거절한 뒤 같은 요청자-대상 조합에 적용할 재요청 제한 시간입니다. 기본값은 60초입니다.

`GuardOrders`
: 간수 지시 메뉴, 시간 간격, 쿨다운, HUD, 기본 장소를 설정합니다.

`GuardOrders.NotificationSoundCooldownSeconds`
: 간수 지시 사운드 최소 간격입니다. 이후 사운드, 핑도 같은 식으로 rate-limit를 적용합니다.

## 빌드

```powershell
dotnet restore
dotnet build -c Release --no-restore
```

## 배포

서버가 실행 중일 때 DLL을 덮어쓰면 이전 인스턴스와 새 인스턴스가 섞여 이상 동작할 수 있습니다.

권장 순서:

```powershell
.\scripts\deploy.ps1 -BuildOnly
```

서버를 끈 뒤:

```powershell
.\scripts\deploy.ps1
```

서버에서 확인:

```text
css_plugins list
css_jb version
css_jb status
```

## 테스트 체크리스트

- CT 간수로 `!지시` 메뉴가 열리는지 확인
- T 상태에서 `!지시`와 `!5`가 막히는지 확인
- 일반 채팅에서 `$6:30 중앙 앉아서`가 `6:30까지 중앙 앉아서`로 뜨는지 확인
- 자유 입력 메뉴 안에서 `$6:30 중앙 앉아서`를 입력하면 원문 그대로 뜨는지 확인
- 일반 채팅에서 `+점프 금지`가 추가명령 줄에 뜨는지 확인
- 일반 채팅에서 `+`만 입력하면 추가명령이 `없음`으로 바뀌는지 확인
- CT 간수로 `!5`를 입력하면 5, 4, 3, 2, 1 카운트다운이 보이는지 확인
- 라운드 시작 후 자동으로 3분 자유시간이 시작되고 시간이 지나면 종료되는지 확인
- 자동 자유시간 중 새 간수 지시를 내리면 자유시간이 즉시 종료되는지 확인
- 라운드 시작 간수 3명 이상 상태에서 간수가 1명으로 줄면 자유시간보다 프리킬 시간이 우선 표시되는지 확인
- 프리킬 시간 중 `!지시`, `+추가명령`, `!5`, 전체 자유시간 시작이 중앙 프리킬 HUD를 덮지 않는지 확인
- T와 CT가 `css_jb team swap <대상>` 요청 후 `css_jb team accept`로 팀을 교환하는지 확인
- 팀교환 거절 후 같은 대상에게 바로 재요청하면 쿨다운으로 막히는지 확인
- `css_jb incidents`에 카운트다운과 반란자 사건이 기록되는지 확인
- 죄수가 간수를 공격하면 빨간색으로 바뀌고 최근 사건에 기록되는지 확인
- 전체 자유시간 중 새 간수 지시가 들어오면 자유시간이 종료되는지 확인
- `css_jb status`가 라운드 상태를 바꾸지 않고 진단만 출력하는지 확인
- `css_jb reset`이 자유시간, 반란자, 카운트다운, 간수 지시 상태를 초기화하는지 확인

## To Do List

### 우선 테스트

- 라운드 시작 자동 3분 자유시간이 정상 시작/종료되는지 실서버 확인
- 자동 자유시간 중 프리킬 조건이 충족되면 프리킬 HUD가 최우선으로 유지되는지 확인
- 프리킬 시간 중 `!지시`, `+추가명령`, `!5`, 전체 자유시간 시작이 HUD를 덮지 않는지 확인
- 팀교환 요청, 수락, 거절, 거절 후 쿨다운이 정상 동작하는지 확인
- 핫리로드 또는 DLL 교체 후 간수 지시 권한/라운드 상태가 꼬이지 않는지 확인

### 다음 구현

- 데모/녹화 안내 또는 명령 연동 방식 조사
- 간수장, 쉴더 등 간수 역할/능력 시스템 설계
- 역할 랜덤 배정 기준과 인원수 제한 설계
- 팀밸 자동 분리 로직 설계
- 팀교환 UI/채팅 안내 문구 개선
- 하드코딩 문구 Localization 정리
- 라디오 키 기반 간수 명령 메뉴 가능 여부 재검토

### 룰 확정 필요

- 팀밸 자유시간 발동 기준
- 팀밸 계산 시 죽은 간수 포함 여부
- 라운드 시작 기준 계산인지 라운드 종료 기준 계산인지
- 고문시간 정확한 발동 조건과 제한
- 프리킬 시간 종료 조건
- 스킨/색상 플러그인과 반란자 빨간색 표시 충돌 여부
